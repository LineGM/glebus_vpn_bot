appenders:
  file:
    kind: file
    path: "glebus_vpn_bot.log"
    encoder:
      pattern: "{d} - {l} - {m}{n}"

  console:
    kind: console
    encoder:
      pattern: "{d} - {l} - {m}{n}"

root:
  level: info
  appenders:
    - file
    - console
